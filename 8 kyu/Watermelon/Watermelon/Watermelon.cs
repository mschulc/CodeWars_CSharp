using System;
public class Watermelon
{
    public static bool Divide(int weight)
    {
        if (weight > 3 && weight % 2 == 0)
            return true;
        else
            return false;
    }
}
